server:
  port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parkmangement
    username: postgres
    password: 1234


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
debug: true
